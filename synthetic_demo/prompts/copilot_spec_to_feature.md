You are GitHub Copilot Chat working in the Bonvoy QE repo.

Task: Convert the attached Jira spec (MBL-TEST-4242) into a Gherkin .feature using ONLY phrases in synthetic_demo/bdd/steps.yaml, prefer testTag names from synthetic_demo/config/selector_catalog.json, parameterize values, and add a negative scenario (cancel at modal). Output valid Gherkin only.

Context: use golden example in synthetic_demo/bdd/features/golden/ModifyRoom_golden.feature to mimic style.
